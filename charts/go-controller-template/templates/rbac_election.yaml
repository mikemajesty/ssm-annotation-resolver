apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: go-controller-template-leader-election-role
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "go-controller-template.labels" . | nindent 4 }}
rules:
  - apiGroups:
      - ""
    resources:
      - configmaps
    verbs:
      - get
      - list
      - watch
      - create
      - update
      - patch
      - delete
  - apiGroups:
      - coordination.k8s.io
    resources:
      - leases
    verbs:
      - get
      - list
      - watch
      - create
      - update
      - patch
      - delete
  - apiGroups:
      - ""
    resources:
      - events
    verbs:
      - create
      - patch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: go-controller-template-leader-election-rolebinding
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "go-controller-template.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: go-controller-template-leader-election-role
subjects:
  - kind: ServiceAccount
    name: {{ include "go-controller-template.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}