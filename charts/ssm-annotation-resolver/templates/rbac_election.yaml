apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "ssm-annotation-resolver.fullname" . }}-leader-election-role
  namespace: {{ include "ssm-annotation-resolver.namespace" . }}
  labels:
    {{- include "ssm-annotation-resolver.labels" . | nindent 4 }}
rules:
  - apiGroups:
      - ""
    resources:
      - configmaps
    verbs:
      - get
      - list
      - watch
      - create
      - update
      - patch
      - delete
  - apiGroups:
      - coordination.k8s.io
    resources:
      - leases
    verbs:
      - get
      - list
      - watch
      - create
      - update
      - patch
      - delete
  - apiGroups:
      - ""
    resources:
      - events
    verbs:
      - create
      - patch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "ssm-annotation-resolver.fullname" . }}-leader-election-rolebinding
  namespace: {{ include "ssm-annotation-resolver.namespace" . }}
  labels:
    {{- include "ssm-annotation-resolver.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "ssm-annotation-resolver.fullname" . }}-leader-election-role
subjects:
  - kind: ServiceAccount
    name: {{ include "ssm-annotation-resolver.serviceAccountName" . }}
    namespace: {{ include "ssm-annotation-resolver.namespace" . }}